<nodes:NodeControl
    x:Class="VBAudioRouter.LocalAudioGraph.Nodes.FileInputNodeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:nodes="using:ShortDev.NodeFlow.WinUI"
    xmlns:utils="using:VBAudioRouter.Utils"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Style="{StaticResource DefaultNodeControlStyle}">

    <nodes:NodeControl.Resources>
        <utils:SliderTimeSpanValueConverter x:Key="SliderTimeSpanValueConverter" />
    </nodes:NodeControl.Resources>

    <Grid HorizontalAlignment="Left">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="15" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" IsReadOnly="True" x:Name="PathDisplay" Margin="0,0,10,0" />
                <Button Grid.Column="1" Content="Browse" Click="Button_Click" x:Name="BrowseButton" />
            </Grid>
        </StackPanel>
        <StackPanel Grid.Column="2">
            <nodes:ConnectorControl x:Name="OutgoingConnectorControl" Margin="0,5,0,5" IsOutgoing="True">
                <nodes:ConnectorControl.RenderTransform>
                    <CompositeTransform TranslateX="18" />
                </nodes:ConnectorControl.RenderTransform>
            </nodes:ConnectorControl>
        </StackPanel>
    </Grid>
</nodes:NodeControl>
