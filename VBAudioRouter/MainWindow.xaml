<windowex:WindowEx
    x:Class="VBAudioRouter.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VBAudioRouter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:windowex="using:WinUIEx" xmlns:ui="using:VBAudioRouter.UI"
    MinWidth="500"
    MinHeight="500"
    PersistenceId="MainWindow"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Title Bar -->
            <RowDefinition Height="*" />
            <!-- App Content -->
        </Grid.RowDefinitions>
        <TitleBar x:Name="DefaultTitleBar" Title="VB Audio Router" Subtitle="Preview">
            <TitleBar.IconSource>
                <FontIconSource Glyph="&#xE7F6;" />
            </TitleBar.IconSource>
        </TitleBar>

        <!-- App content -->
        <NavigationView Grid.Row="1"
                        Style="{StaticResource MainNavigationViewStyle}"
                        IsSettingsVisible="False"
                        SelectionChanged="NavigationView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Audio Graph" Tag="LocalAudioGraph" IsSelected="True">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEF90;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Speakers" Tag="LocalAudioMix">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xe7f5;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="HiQnet" Tag="HiQnet">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8AF;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="MainFrame" />
        </NavigationView>
    </Grid>
</windowex:WindowEx>
